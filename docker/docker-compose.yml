services:
  server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "${HOST_PORT:-127.0.0.1:8080}:8080"
    env_file:
      - ../.env
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      LOG_FILE_PATH: /app/logs/sshvault.log
    volumes:
      - keys:/app/keys
      - logs:/app/logs
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    networks:
      - backend
      - proxy

  backup:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    entrypoint: ["./sshvault-cli", "backup", "auto"]
    env_file:
      - ../.env
    environment:
      BACKUP_DIR: /backups
      BACKUP_INTERVAL: "${BACKUP_INTERVAL:-24h}"
      BACKUP_RETENTION: "${BACKUP_RETENTION:-7}"
    volumes:
      - backups:/backups
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    networks:
      - backend

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sshvault
      POSTGRES_USER: sshvault
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sshvault"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - backend

networks:
  backend:
    internal: true
  proxy:

volumes:
  pgdata:
  backups:
  keys:
  logs:
